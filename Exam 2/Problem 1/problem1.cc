// Copyright 2022 CSCE240
// Exam 2 problem1.cc
#include<problem1.h>
#include<iostream>

// implement the Remove function here
void Remove(int an_array[], int array_size, int to_remove) {
    int counter = 0;
    int shift = 0;
    auto test = std::remove(std::begin(an_array), std::end(an_array), 3);
}
